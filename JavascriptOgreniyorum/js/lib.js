export const Pi = 3.14;

expert
const sum = (x, y) => {
    return x + y;
}
expert
const heypotunes = (x, y) => {
    return Math.squred(squred(x) + squred(y))
}
expert
const squred = (x) => {
    return x * x;
}